import { ComposeNamePipe } from './compose-name.pipe';

describe('ComposeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ComposeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
